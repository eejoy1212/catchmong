import 'package:get/get.dart';
// import 'package:google_maps_flutter/google_maps_flutter.dart';

class LocationController extends GetxController {
  // final Rx<LatLng> initialPosition =
  //     LatLng(37.5665, 126.9780).obs; // 서울 초기 위치 설정
  // final Rx<LatLng?> selectedLocation = Rx<LatLng?>(null); // 선택된 위치
  // final RxSet<Marker> markers = <Marker>{}.obs;
  // GoogleMapController? mapController;

  // void updateLocation(LatLng position) {
  //   selectedLocation.value = position;
  //   markers.value = {
  //     Marker(
  //       markerId: MarkerId('selected-location'),
  //       position: position,
  //     ),
  //   };
  // }

  // void onMapCreated(GoogleMapController controller) {
  //   mapController = controller;
  //   // 초기 위치로 이동
  //   controller.animateCamera(CameraUpdate.newLatLng(initialPosition.value));
  // }
}
